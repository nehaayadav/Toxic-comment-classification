# Toxic Comment Classification
Dataset - https://www.kaggle.com/c/jigsaw-toxic-comment-classification-challenge

## Project Structure

```
toxic-comment-classifier
│
├── notebooks/
│   └── eda_feature_engineering.ipynb      # EDA, feature engineering, and visualization
│
├── src/
│   └── preprocessing_text.py              # Text cleaning and preprocessing functions
│
├── models/                                # Saved vectorizers and trained models
├── features/                              # Generated feature matrices
├── data/                                  # Dataset files
├── embeddings/                            # Pretrained embeddings (GloVe)
│
├── README.md                              # Project documentation
├── requirements.txt                       # Python dependencies
└── .gitignore                             # Git ignored files
```

Note: Large files such as datasets, generated features, embeddings, and
virtual environments are excluded from the repository using .gitignore.
They can be generated by running the notebooks.

## Toxic Comment EDA + Feature Engineering

This project is based on the **Kaggle Toxic Comment Classification Challenge** dataset.  
It focuses on:
- Basic dataset introduction (shape, duplicates, missing values)  
- Exploratory Data Analysis (EDA)  
- Feature Engineering (manual features + sentiment features)  
- Advanced text encodings (TF-IDF, Word2Vec, GloVe)  
- High-dimensional visualization (t-SNE)

### Dataset
- 159,571 Wikipedia comments
- 6 labels: toxic, severe_toxic, obscene, threat, insult, identity_hate

### Key Insight
The dataset is **highly imbalanced**. Most comments are non-toxic, while *threat* and *identity_hate* are very rare.

---
